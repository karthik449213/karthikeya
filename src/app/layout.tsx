import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains-mono",
});

export const metadata: Metadata = {
  title: "Karthikeya Muchintala",
  description: "Professional web developer specializing in rapid website development. Delivering high-impact websites and web applications with lightning-fast turnaround.",
  keywords: "web developer, rapid development, React, JavaScript, Python, Tailwind CSS, professional websites, web applications",
  authors: [{ name: "Karthikeya Muchintala" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
